loadbalancer:
  host: <%= loadbalancer_host %>
  port: 5000
  
hypervisor:
  port: 5000
  user: student

ssh:
  timeout: 5
  delay: 1
  keyfile: /etc/vzsandbox_keys/cpMinimal.id_rsa

general:
  tmpdir: /vzsandbox-shell-tmp
